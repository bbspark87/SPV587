<view class="container">
    <image class="container-btn" mode="aspectFill" src="{{src}}"></image>
    <view class="container-txt-icon">
        <image class="img-icon" hidden="{{!src}}" src="{{src}}" mode="aspectFill"></image>
        <text class="txt-button">{{title}}</text>
    </view>
</view>